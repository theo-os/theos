package(default_visibility = ["//visibility:public"])

load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(
    name = "loader",
    srcs = glob(["**/*.rs"]),
    crate_root = "loader.rs",
    crate_name = "loader",
    edition = "2024",
    deps = [
        "@crates//:goblin",
    ],
)
